<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fkit.mapper.UserMapper">
    <insert id="save" parameterType="org.fkit.domain.User" useGeneratedKeys="true">
        INSERT INTO TB_USER(NAME1,sex,age) VALUES (#{name},#{sex},#{age})
    </insert>
</mapper>

<!--
操纵数据库的SQL语句
namespace:包名+类名
parameterType：插入时使用的参数类型
useGeneratedKeys="true"：表示使用数据库的自动增长策略
#{name},#{sex},#{age}：表示获取参数中对象的name、sex、age属性值，与User对象的属性名一致
-->