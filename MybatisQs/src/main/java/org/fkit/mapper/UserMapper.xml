<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fkit.mapper.UserMapper">

    <insert id="save" parameterType="org.fkit.domain.User" useGeneratedKeys="true">
        INSERT INTO TB_USER(NAME,sex,age) VALUES (#{name},#{sex},#{age})
    </insert>

   <insert id="saveUser" parameterType="org.fkit.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO TB_USER(NAME,sex,age) VALUES (#{name},#{sex},#{age})
    </insert>

   <select id="selectUser" parameterType="int" resultType="org.fkit.domain.User">
        SELECT * FROM tb_user WHERE ID=#{id}
    </select>

    <update id="modifyUser" parameterType="org.fkit.domain.User">
        UPDATE TB_USER
        SET name=#{name},sex=#{sex},age=#{age}
        WHERE id=#{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM TB_USER WHERE id=#{id}
    </delete>
</mapper>

<!--
操纵数据库的SQL语句
namespace:包名+类名
parameterType：插入时使用的参数类型
useGeneratedKeys="true"：表示使用数据库的自动增长策略
#{name},#{sex},#{age}：表示获取参数中对象的name、sex、age属性值，与User对象的属性名一致
keyProperty=“id":将插入数据生成的主键设置到User对象的id当中
resultType="user":表示查询语句返回一个User对象
-->